# CodeRabbit Setup

CodeRabbit - AI-powered code review assistant для автоматического ревью pull requests.

## Установка

1. **Установить CodeRabbit GitHub App:**
   - Перейти на https://github.com/apps/coderabbitai
   - Нажать "Install" или "Configure"
   - Выбрать репозиторий `chatman-media/tauri-next15-ssr`
   - Подтвердить установку

2. **Настройка уже готова:**
   - Конфигурация находится в `.coderabbit.yaml`
   - CodeRabbit автоматически начнет ревьюить PR после установки

## Возможности

### Автоматический Review
- ✅ Проверка TypeScript типов и React best practices
- ✅ Анализ безопасности Rust кода
- ✅ Проверка тестов (E2E и unit)
- ✅ Валидация CI/CD workflows
- ✅ Performance recommendations

### Path-based Instructions
Настроены специальные инструкции для разных типов файлов:

- **TypeScript/React** (`src/**/*.{ts,tsx}`):
  - Строгость типов
  - React hooks правила
  - Performance оптимизации

- **E2E тесты** (`e2e/**/*.spec.ts`):
  - Покрытие критичных сценариев
  - Правильные локаторы
  - Assertions и timeouts

- **Unit тесты** (`**/*.test.{ts,tsx}`):
  - Coverage критичной логики
  - Testing Library best practices

- **Rust код** (`src-tauri/**/*.rs`):
  - Безопасность
  - Обработка ошибок
  - Tauri API usage

- **GitHub Actions** (`.github/workflows/**`):
  - Pipeline корректность
  - Versions и secrets

### Ignore Patterns
CodeRabbit игнорирует:
- Документацию (*.md)
- Lock файлы
- Build артефакты (dist, .next, target)
- node_modules
- Storybook static

## Использование

### Автоматическое ревью
После создания PR, CodeRabbit автоматически:
1. Анализирует изменения
2. Оставляет комментарии с рекомендациями
3. Создает summary с high-level обзором
4. Добавляет стихотворение в конце ревью 😊

### Команды в PR
Используй команды в комментариях PR:

```bash
# Попросить ревью определенного файла
@coderabbitai review src/app/page.tsx

# Попросить объяснение изменений
@coderabbitai explain

# Попросить написать тесты
@coderabbitai generate tests

# Игнорировать комментарий
@coderabbitai ignore

# Resolve все замечания
@coderabbitai resolve
```

### Skip Review
Если не нужен ревью для конкретного PR:
- Добавь `WIP`, `DO NOT REVIEW` или `DRAFT` в title PR

## Конфигурация

### Основные настройки
```yaml
language: ru                    # Комментарии на русском
reviews:
  review_status: auto          # Автоматический уровень ревью
  auto_review:
    enabled: true              # Включить авто-ревью
  request_changes_workflow: false  # Не блокировать PR
```

### Изменение конфигурации
Редактируй `.coderabbit.yaml`:
```bash
# После изменений закоммить
git add .coderabbit.yaml
git commit -m "chore: update coderabbit config"
git push
```

Изменения применятся к следующим PR автоматически.

## FAQ

### Как отключить CodeRabbit для конкретного файла?
Добавь паттерн в `.coderabbit.yaml`:
```yaml
ignore:
  - "path/to/file.ts"
```

### Как запросить повторное ревью?
Просто напиши в PR:
```
@coderabbitai review
```

### Можно ли изменить язык комментариев?
Да, измени в `.coderabbit.yaml`:
```yaml
language: en  # для английского
```

## Полезные ссылки

- [CodeRabbit Docs](https://docs.coderabbit.ai/)
- [Configuration Guide](https://docs.coderabbit.ai/guides/configure-coderabbit)
- [Commands Reference](https://docs.coderabbit.ai/guides/review-instructions)
